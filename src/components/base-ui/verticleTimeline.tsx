type TimelineItem = { id?: string; dateTime?: string; date?: string; title?: string; label?: string; description?: string }
export function VerticalTimeline({ items = [], activeIndex = 0, height = 800 }: { items?: TimelineItem[]; activeIndex?: number; height?: number }) { return <div className='relative w-full max-w-[800px] overflow-auto rounded-lg border p-4' style={{ maxHeight: height }}><span aria-hidden className='absolute bottom-6 left-[23px] top-6 w-px bg-border' />{items.map((item, index) => <div key={item.id ?? index} className='relative flex gap-4 pb-8 last:pb-0'><div className='relative z-10 flex w-4 shrink-0 justify-center'><span className={`mt-1 h-3 w-3 rounded-full border-2 ${index <= activeIndex ? 'border-primary bg-primary' : 'border-muted-foreground bg-background'}`} /></div><div className='min-w-0'><time dateTime={item.dateTime} className='text-xs text-muted-foreground'>{item.date}</time><h3 className='font-semibold'>{item.title ?? item.label}</h3><p className='text-sm text-muted-foreground'>{item.description}</p></div></div>)}</div> }
export default VerticalTimeline
