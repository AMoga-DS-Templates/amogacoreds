import { Card, CardContent } from '@/components/ui/card'

type Item = { name?: string; label?: string; value?: string | number; change?: string | number; changeType?: 'positive' | 'negative' | 'neutral' }
export function StatswithTrending({ data, summary = [] }: { data?: Item[]; summary?: Item[] }) { const items = data ?? summary; return <div className='grid w-full grid-cols-1 gap-px overflow-hidden rounded-lg border sm:grid-cols-2 lg:grid-cols-4'>{items.map((item, index) => <Card key={index} className='rounded-none border-0 border-r border-b shadow-none'><CardContent className='flex min-w-0 flex-wrap items-baseline justify-between gap-x-4 gap-y-2 p-4'><div className='bg-gradient-to-r from-primary via-chart-1 to-chart-2 bg-clip-text text-sm font-semibold text-transparent'>{item.name ?? item.label ?? `Metric ${index + 1}`}</div>{item.change ? <div className={item.changeType === 'negative' ? 'text-[11px] text-destructive' : item.changeType === 'positive' ? 'text-[11px] text-primary' : 'text-[11px] text-muted-foreground'}>{item.change}</div> : null}<div className='w-full text-xl font-medium'>{item.value ?? 0}</div></CardContent></Card>)}</div> }
export default StatswithTrending
