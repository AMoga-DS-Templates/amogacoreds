type TimelineItem = { id?: string; dateTime?: string; date?: string; title?: string; label?: string; description?: string }
export function HorizontalAlternateTimeline({ items = [], activeIndex = 0, height = 260 }: { items?: TimelineItem[]; activeIndex?: number; height?: number }) { return <div className='w-full max-w-[800px] overflow-x-auto overflow-y-hidden rounded-lg border p-4' style={{ minHeight: height }}><div className='flex min-w-[640px] items-start'>{items.map((item, index) => <div key={item.id ?? index} className='relative flex flex-1 flex-col items-center text-center'><div className='flex w-full items-center'><span className={`ml-1 h-3 w-3 shrink-0 rounded-full border-2 ${index <= activeIndex ? 'border-primary bg-primary' : 'border-muted-foreground bg-background'}`} />{index < items.length - 1 ? <span className='h-px flex-1 bg-border' /> : null}</div><div className='mt-3 px-2'><time dateTime={item.dateTime} className='text-xs text-muted-foreground'>{item.date}</time><h3 className='font-semibold'>{item.title ?? item.label}</h3><p className='text-sm text-muted-foreground'>{item.description}</p></div></div>)}</div></div> }
export default HorizontalAlternateTimeline
