import type { ElementType, ReactNode } from 'react'

const headingClasses = { h1: 'scroll-m-20 text-4xl font-extrabold tracking-tight lg:text-5xl', h2: 'scroll-m-20 border-b pb-2 text-3xl font-semibold tracking-tight', h3: 'scroll-m-20 text-2xl font-semibold tracking-tight', h4: 'scroll-m-20 text-xl font-semibold tracking-tight' }
export function Heading({ text, level = 'h2', children }: { text?: string; level?: 'h1' | 'h2' | 'h3' | 'h4' | string; children?: ReactNode }) { const value = text ?? children; const className = headingClasses[level as keyof typeof headingClasses] ?? headingClasses.h2; const Tag = (['h1', 'h2', 'h3', 'h4'].includes(level) ? level : 'h2') as ElementType; return <Tag className={className}>{value}</Tag> }
export function Blockquote({ text, cite, children }: { text?: string; cite?: string; children?: ReactNode }) { return <figure><blockquote className='mt-6 border-l-2 pl-6 italic text-muted-foreground'>{text ?? children}</blockquote>{cite ? <figcaption className='mt-1 pl-6 text-sm text-muted-foreground'>— {cite}</figcaption> : null}</figure> }
export function InlineCode({ code, children }: { code?: string; children?: ReactNode }) { return <code className='relative rounded bg-muted px-[0.3rem] py-[0.2rem] font-mono text-sm font-semibold'>{code ?? children}</code> }
