import { Card, CardContent, CardFooter } from '@/components/ui/card'

type Item = { name?: string; label?: string; value?: string | number; change?: string | number; changeType?: 'positive' | 'negative'; href?: string; linkLabel?: string }
export function StatswithLinks({ data, summary = [] }: { data?: Item[]; summary?: Item[] }) { const items = data ?? summary; return <div className='mx-auto w-full max-w-[1000px]'><dl className='mx-auto grid w-full justify-center gap-4 [grid-template-columns:repeat(auto-fit,minmax(min(100%,14rem),14rem))]'>{items.map((item, index) => <Card key={index} className='min-w-0 p-0 shadow-sm'><CardContent className='p-4'><dd className='flex items-start justify-between gap-3'><span className='bg-gradient-to-r from-primary via-chart-1 to-chart-2 bg-clip-text text-sm font-extrabold text-transparent'>{item.name ?? item.label}</span><span className={item.changeType === 'negative' ? 'text-sm text-red-700' : 'text-sm text-emerald-700'}>{item.change}</span></dd><dd className='mt-1 text-3xl font-semibold tabular-nums'>{item.value ?? 0}</dd></CardContent><CardFooter className='justify-end p-0'><a href={item.href ?? '#'} className='px-4 py-3 text-sm font-medium text-primary'>{item.linkLabel ?? 'View more'} →</a></CardFooter></Card>)}</dl></div> }
export default StatswithLinks
