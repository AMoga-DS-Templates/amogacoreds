import { TrendingDown, TrendingUp } from 'lucide-react'
import { Badge } from '@/components/ui/badge'
import { Card, CardContent, CardTitle } from '@/components/ui/card'

type Item = { metric?: string; current?: string | number; previous?: string | number; difference?: string | number; trend?: 'up' | 'down'; name?: string; label?: string; value?: string | number; change?: string | number }
export function StatswithBorders({ stats, summary = [] }: { stats?: Item[]; summary?: Item[] }) { const items = stats ?? summary; return <div className='grid w-full grid-cols-[repeat(auto-fit,minmax(min(100%,13rem),1fr))] gap-px overflow-hidden rounded-lg'>{items.map((item, index) => { const up = item.trend !== 'down'; return <Card key={index} className='rounded-none border-0 px-0 py-0 shadow-sm'><CardContent className='p-4'><CardTitle className='bg-gradient-to-r from-primary via-chart-1 to-chart-2 bg-clip-text text-sm font-extrabold text-transparent'>{item.metric ?? item.name ?? item.label ?? `Metric ${index + 1}`}</CardTitle><div className='mt-2 flex flex-wrap items-center gap-2'><span className='text-2xl font-semibold text-primary'>{String(item.current ?? item.value ?? 0)}</span><span className='text-sm text-muted-foreground'>from {String(item.previous ?? '-')}</span><Badge variant='outline' className={up ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800'}>{up ? <TrendingUp className='mr-1 h-4 w-4' /> : <TrendingDown className='mr-1 h-4 w-4' />}{String(item.difference ?? item.change ?? '0%')}</Badge></div></CardContent></Card>})}</div> }
export default StatswithBorders
