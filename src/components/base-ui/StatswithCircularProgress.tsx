import { ExternalLink } from 'lucide-react'
import { Card, CardContent } from '@/components/ui/card'

type Item = { name?: string; label?: string; capacity?: number; current?: string | number; allowed?: string | number; value?: string | number; percentage?: number }
export function StatswithCircularProgress({ title = 'Plan overview', description = 'You are currently on the starter plan.', linkLabel, linkUrl, data, summary = [] }: { title?: string; description?: string; linkLabel?: string; linkUrl?: string; data?: Item[]; summary?: Item[] }) { const items = data ?? summary; return <div className='w-full'><h2 className='text-xl font-medium'>{title}</h2><p className='mt-1 text-sm leading-6 text-muted-foreground'>{description} {linkLabel && linkUrl ? <a href={linkUrl} className='inline-flex items-center gap-1 text-primary hover:underline'>{linkLabel}<ExternalLink className='size-4' /></a> : null}</p><dl className='mt-6 grid grid-cols-[repeat(auto-fit,minmax(min(100%,14rem),1fr))] gap-4'>{items.map((item, index) => { const capacity = Math.max(0, Math.min(100, Number(item.capacity ?? item.percentage ?? 0))); return <Card key={index} className='min-w-0'><CardContent className='flex items-center gap-4 p-4'><div className='relative flex h-20 w-20 items-center justify-center rounded-full' style={{ background: `conic-gradient(hsl(var(--primary)) ${capacity}%, hsl(var(--muted)) 0)` }}><div className='flex h-16 w-16 items-center justify-center rounded-full bg-card text-sm font-medium'>{capacity}%</div></div><div><dt className='bg-gradient-to-r from-primary via-chart-1 to-chart-2 bg-clip-text text-sm font-extrabold text-transparent'>{item.name ?? item.label ?? `Metric ${index + 1}`}</dt><dd className='text-sm text-muted-foreground'>{String(item.current ?? item.value ?? 0)} of {String(item.allowed ?? 100)} used</dd></div></CardContent></Card>})}</dl></div> }
export default StatswithCircularProgress
